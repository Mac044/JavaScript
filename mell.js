// Create variables `name`,`age`,`shortDescription` and `favWebDevTopic` and use concatenation to turn the values in this variable to a full sentence. Use the _alert()_ to print it to screen.

const name = "Agbomeji subomi";

const age = "26";

const shortDescription = "I'm a web developer";

const favWebDevTopic = "bootstrap";

const infos = (Name) + (age) + (shortDescription) + (favWebDevTopic)

alert(infos)