// 8. Write a script that prompt the user to enter side a, side b, and side c of the triangle and and calculate the perimeter of triangle (perimeter = a + b + c)
//    Enter side a: 5
//    Enter side b: 4
//    Enter side c: 3
//    The perimeter of the triangle is 12

const x = prompt("Enter side a of the triangle");

const y = prompt("Enter side b of the triangle");

const w = prompt ("Enter side c of the triangle");

const perimeter = parseInt(x) + parseInt(y) + parseInt(w);

const ans = perimeter;


alert(ans)