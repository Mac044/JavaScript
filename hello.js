// 3. Using console.log() print out the following statement:
//`The quote 'There is no exercise better for the heart than reaching down and lifting people up.' by John Holmes teaches us to help one another.`

const C = "There is no exercise better for the heart than reaching down and lifting people up by John Holmes teaches us to help one another.";

console.log(C);

// 4. Check if typeof '10' is exactly equal to 10. If not make it exactly equal.

const b = '10';

console.log(b);

// 5. Check if parseFloat('9.8') is equal to 10 if not make it exactly equal with 10.

const d = '9.8';

console.log(d)

const e = parseFloat(d) + parseFloat(0.2);

console.log(e)


// 6. Concatenate the following strings `My name is` and `web dev`

const R = "My name is";

const rR = "web dev";

const info = (R) + (rRR);

console.log(info)

